# Pin chặt version — vnstock đã từng đổi API đột ngột giữa các bản major (v3 -> v4),
# không dùng "-U"/version mở trong production. Nâng cấp version qua PR riêng có kiểm tra thủ công.
vnstock==4.0.5
pandas>=2.0
duckdb>=1.0
tenacity>=8.0
